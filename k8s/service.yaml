apiVersion: v1
kind: Service
metadata:
  name: shopapp-service
spec:
  type: NodePort
  selector:
    app: shopapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
